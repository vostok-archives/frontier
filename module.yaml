full-build *default:
  deps:
    - vstk.clusterclient.transport.http
    - vstk.clusterclient
    - vstk.airlock.client
    - vstk.core
    - vstk.hosting.core
    - vstk.hosting
    - vstk.instrumentation.aspnetcore
    - vstk.logging.serilog

  build:
    target: Vstk.Frontier.sln
    configuration: Release
    tool:
      name: dotnet
